<%= simple_form_for(@mealplan) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :meal_id %>
    <%= f.input :client_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
