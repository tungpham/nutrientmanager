<%= simple_form_for(@nutritionist) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :something %>
    <%= f.input :user_id %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
